<% if @comment.errors.any? %>
  alert("<%= raw @comment.errors.full_messages.join("\n") %>")
<% else %>
  var $current_comment = $("#comment_<%= @comment.id %>")
  $current_comment.after("<%= j render @comment %>")
  $current_comment.first().remove()
<% end %>
