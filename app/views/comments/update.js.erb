<% if @comment.errors.any? %>
  alert("<%= raw @comment.errors.full_messages.join("\n") %>")
<% else %>
  $("#comment_<%= @comment.id %>").after("<%= j render @comment %>")
  $("#comment_<%= @comment.id %>").first().remove()
<% end %>
